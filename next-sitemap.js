/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://cafeterasportatiles.online',
  generateRobotsTxt: true,
  // Esto fuerza a que los archivos se escriban en la carpeta de exportación:
  outDir: './out',
}
